{
  "name": "@senlay/open-network",
  "version": "0.1.0",
  "description": "Open sensor protocol and SDK for verified physical context",
  "type": "module",
  "main": "src/index.js",
  "exports": "./src/index.js",
  "files": ["src", "protocol", "gateway", "adapters", "python/*.py", "python/tests/*.py", "docs"],
  "scripts": {
    "check": "node --check src/index.js && node --check src/protocol.js && node --check src/signing.js && node --check src/client.js && node --check gateway/server.js && node --check gateway/mqtt-bridge.js && node --check adapters/example-sensor.js && python3 -m py_compile python/senlay_client.py python/simulator.py python/publish_reading.py",
    "test": "node --test test/*.test.js && npm run test:python",
    "test:python": "python3 -m unittest discover -s python/tests -p 'test_*.py'",
    "gateway": "node gateway/server.js",
    "mqtt-bridge": "node gateway/mqtt-bridge.js",
    "example": "node adapters/example-sensor.js"
  },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0",
  "dependencies": {
    "mqtt": "^5.10.0"
  }
}
